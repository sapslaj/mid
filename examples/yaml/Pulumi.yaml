name: provider-mid
runtime: yaml
plugins:
  providers:
    - name: mid
      path: ../../bin

resources:
  vim:
    type: mid:resource:Package
  emacs:
    type: mid:resource:Package
    properties:
      name: emacs
      state: absent

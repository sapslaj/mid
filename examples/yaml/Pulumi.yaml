name: provider-mid
runtime: yaml
plugins:
  providers:
    - name: mid
      path: ../../bin
config:
  mid:connection:user: root
  mid:connection:password: hunter2
  mid:connection:host: localhost
  mid:connection:port: 2222
resources:
  vim:
    type: mid:resource:Package
  emacs:
    type: mid:resource:Package
    properties:
      name: emacs
      state: absent
